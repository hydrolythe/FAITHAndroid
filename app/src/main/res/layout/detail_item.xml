<?xml version="1.0" encoding="utf-8"?>
<layout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    >

    <data>
        <variable
            name="detail"
            type="be.hogent.faith.domain.models.detail.Detail" />
        <variable
            name="clickListener"
            type="be.hogent.faith.faith.backpackScreen.DetailListener" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content">

        <ImageView
            android:id="@+id/backpack_detail"
            android:background="@drawable/rv_item_detail_background"
            android:layout_width="100dp"
            android:layout_height="100dp"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@+id/backpack_detail_delete"
            app:srcCompat="@drawable/event_detail_text" />

        <ImageView
            android:id="@+id/backpack_detail_delete"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:layout_constraintEnd_toEndOf="@+id/backpack_detail"
            app:layout_constraintTop_toTopOf="parent"
            app:srcCompat="@drawable/ic_icoon_verwijder" />
    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>